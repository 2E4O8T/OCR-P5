@model IEnumerable<EVSec.Models.Reparations>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateDisponibilite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoutReparation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inventaire)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateDisponibilite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoutReparation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Inventaire.Finition)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReparationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReparationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReparationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
